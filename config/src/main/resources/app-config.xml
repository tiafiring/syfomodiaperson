<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no http://maven.adeo.no/xsd/appconfig.xsd">

    <name>modiasyfofront</name>

    <selftest>
        <path>sykefravaer/internal/selftest</path>
        <humanReadablePath>sykefravaer/internal/selftest</humanReadablePath>
    </selftest>

    <artifacts>
        <ear groupId="no.nav.sbl.dialogarena" artifactId="modiasyfofront-ear"/>
    </artifacts>

    <security serviceUserResourceAlias="srvmodiasyfofront" />

    <resources>
        <applicationProperties alias="modiasyfofront.properties"/>

        <baseUrl alias="modapp" mapToProperty="modapp"/>
        <rest alias="modiasyfoapi" mapToProperty="sykefravaerapi.fss"/>
        <rest alias="iadecorator-js" mapToProperty="iadecorator"/>
        <rest alias="moteapifss" mapToProperty="moteapifss"/>

        <baseUrl alias="isso-host"/>
        <baseUrl alias="isso-jwks"/>
        <baseUrl alias="isso-issuer"/>
        <credential alias="isso-rp-user"/>
    </resources>

    <loadBalancer isAlive="/sykefravaer/internal/isAlive" />
</application>
